// submit button handler for registering a new user
const onRegister = (data) => {
    createUser(data)
};

// submit button handler for deleting a user
const onDelete = (data) => {
    deleteUser(data)
};